import os

# путь к файлу
file_path = "test.txt"

if os.path.exists(file_path):   # проверим, существует ли файл
    os.remove(file_path)        # удаляем
    print(f"Файл {file_path} удалён")
else:
    print("Файл не найден")


🔑 Основные возможности os:
📂 Работа с файлами и папками

os.remove(path) — удалить файл.

os.rmdir(path) — удалить пустую папку.

os.mkdir(path) — создать папку.

os.makedirs(path) — создать папки рекурсивно (a/b/c).

os.rename(src, dst) — переименовать или переместить файл/папку.

os.listdir(path=".") — список файлов в папке.

os.path.exists(path) — проверить, существует ли файл/папка.

🌍 Пути

os.path.join(a, b) — правильное соединение путей (под Windows \, под Linux /).

os.path.abspath(path) — абсолютный путь.

os.path.basename(path) — имя файла.

os.path.dirname(path) — имя папки.

os.path.split(path) — разделить путь на (папка, файл).

os.path.splitext(path) — разделить имя файла и расширение.

⚙️ Переменные окружения

os.environ — словарь переменных окружения.

os.getenv("PATH") — получить переменную окружения.

💻 Система

os.name — имя ОС (nt для Windows, posix для Linux/Mac).

os.system("команда") — выполнить системную команду.

os.cpu_count() — количество ядер процессора.

os.getlogin() — имя пользователя.

os.getpid() — PID текущего процесса.

📌 Текущая директория

os.getcwd() — текущая рабочая папка.

os.chdir(path) — сменить рабочую папку.